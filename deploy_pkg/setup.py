from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='It heng a model to predict wether the water is consumable or not',
    author='Ninad',
    license='MIT',
)
